version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - echo "AUTH_SECRET=$AUTH_SECRET" >> .env
        - echo "AUTH_TRUST_HOST=$AUTH_TRUST_HOST" >> .env
        - echo "OPENROUTER_API_KEY=$OPENROUTER_API_KEY" >> .env
        - echo "DATABASE_URL=$DATABASE_URL" >> .env
        - echo "AUTH_GOOGLE_ID=$AUTH_GOOGLE_ID" >> .env
        - echo "AUTH_GOOGLE_SECRET=$AUTH_GOOGLE_SECRET" >> .env
        - echo "STRIPE_SECRET_KEY=$STRIPE_SECRET_KEY" >> .env
        - echo "STRIPE_WEBHOOK_SECRET=$STRIPE_WEBHOOK_SECRET" >> .env
        - echo "NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=$NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY" >> .env
        - |
          if [ "${AWS_BRANCH}" = "master" ]; then
            echo "NEXT_PUBLIC_APP_URL=https://optifyseo.com" >> .env
            echo "NEXTAUTH_URL=https://optifyseo.com" >> .env
            echo "AUTH_URL=https://optifyseo.com" >> .env

          else
            SAFE_BRANCH=$(echo "${AWS_BRANCH}" | sed 's/\//-/g')
            echo "NEXT_PUBLIC_APP_URL=https://${SAFE_BRANCH}.${AWS_APP_ID}.amplifyapp.com" >> .env
            echo "NEXTAUTH_URL=https://${SAFE_BRANCH}.${AWS_APP_ID}.amplifyapp.com" >> .env
            echo "AUTH_URL=https://${SAFE_BRANCH}.${AWS_APP_ID}.amplifyapp.com" >> .env
          fi
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
